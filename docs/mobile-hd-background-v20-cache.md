# Cache separation

The v20 client and stylesheet use the release key `20260809-mobile-hd-background-v20-2`. The new MP4 and poster use unique immutable filenames, so an older Safari tab cannot mistake the prior upscaled scene for the true-HD source.
