# Decoded dimensions

The production video must decode to 1440 × 2560. The production verifier checks decoded dimensions with FFprobe and again through the browser's `videoWidth` and `videoHeight` properties.
