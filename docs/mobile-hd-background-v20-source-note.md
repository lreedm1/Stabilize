# Effective-resolution finding

The former portrait media was not being blurred by CSS. Its 2160 × 3840 dimensions came from scaling a 512 × 908 preview, so the visual detail remained preview-grade. The v20 release therefore replaces the original source rather than applying another resize or sharpening pass.
