# Mobile HD v20 rollout boundary

The new video is an additive layer. The prior poster and canvas remain unchanged underneath it, so removing the v20 video element, client, styles, assets, and finalizer import restores the previous behavior without data migration or account impact.
