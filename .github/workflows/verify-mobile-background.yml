name: Historical coherent mobile background verifier

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the canonical verifier
        run: echo 'The active mobile release is verified by verify-mobile-video.yml.'
