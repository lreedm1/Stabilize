name: Export reasoning impact worktree

on:
  push:
    branches:
      - agent/reasoning-effort-impact
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    if: github.event_name != 'pull_request' || github.head_ref == 'agent/reasoning-effort-impact'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v7
        with:
          ref: agent/reasoning-effort-impact
          persist-credentials: false

      - name: Remove Git metadata
        run: rm -rf .git

      - name: Upload worktree
        uses: actions/upload-artifact@v4
        with:
          name: reasoning-impact-worktree
          path: .
          include-hidden-files: true
          retention-days: 1
